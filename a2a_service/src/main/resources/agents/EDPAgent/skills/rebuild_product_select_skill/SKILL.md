---
name: rebuild_product_select_skill
description: >
  与用户多轮确认理财产品选择及购买金额，输出结构化选品结果。
  触发词：我选第X个、买这个、选XXXXX、我要买XX元、购买第X个、购买XX产品。
  当用户表达购买/选择意图且上文存在产品推荐列表时，必须先使用本 skill 确认选品信息，禁止直接跳到资金筹划。
  不要用于：产品推荐查询、资金筹划、账户余额查询。
---

# 产品选择 Skill

## 职责

从上文产品列表中通过多轮对话（最多 5 轮）收集用户的选择和购买金额，
处理意图漂移，最终回复包含完整选品信息的结构化确认。

## 工具白名单（严格）

只允许调用以下工具：
- `ask_user`：向用户追问缺失信息（如未选产品、未填金额）

禁止调用 `recommend_product`、`buy_wealth` 或其他非白名单工具。

选品阶段不要直接发起业务工作流调用；只负责确认用户选择结果并组织结构化确认回复。

## 执行流程

### 第一步：获取产品列表

从当前对话上文中查找已展示的产品列表。

- 若上文中存在产品列表 → 直接使用，继续下一步。
- 若上文中没有产品列表 → 直接回复"暂无可选产品信息，请先获取理财推荐"，结束。

### 第二步：解析用户当前输入

尝试从用户输入中提取：
- 产品选择：序号、产品代码、产品名称关键词
- 购买金额：数字 + 单位，"5万"→ 50000，"5000元"→ 5000

### 第三步：追问缺失信息

- 缺少产品选择时，调用 `ask_user`
- 缺少购买金额时，调用 `ask_user`
- 金额无效时，调用 `ask_user`

### 第四步：确认并输出结构化选品结果

收集到完整的产品选择和购买金额后，先向用户展示并请求确认。

确认后输出结构化结果，必须包含：
- 产品名称
- 产品代码
- 产品类型
- 预期年化收益
- 风险等级
- 购买金额
- 理财卡尾号

## 规则约束

- 产品列表来自上文对话，不要自行调用任何查询工具。
- `ask_user` 累计调用不超过 5 次；超出后直接给出超时提示并结束。
- 最终结构化回复必须包含 `productCode`，不可省略。
- 不要替用户做最终决策，必须由用户明确说出选择。
