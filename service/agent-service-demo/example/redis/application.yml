spring:
  application:
    name: demo-redis-agent-service
  config:
    import:
      - optional:classpath:application-base.yml
      - optional:classpath:application-base_local.yml
      - optional:classpath:application-redis-checkpointer.yml
